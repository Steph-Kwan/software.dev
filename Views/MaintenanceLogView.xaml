<UserControl x:Class="TrackMaintenance.Views.MaintenanceLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- header -->
            <RowDefinition Height="*"/>      <!-- data grid -->
            <RowDefinition Height="Auto"/>   <!-- footer -->
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="24,14"
                BorderBrush="{StaticResource BorderLight}" BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="保养记录" Style="{StaticResource PageTitle}" VerticalAlignment="Center"/>
                <TextBlock x:Name="CountText" HorizontalAlignment="Right" VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondary}" FontSize="13" FontWeight="SemiBold"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="16" Background="White" CornerRadius="10"
                BorderBrush="{StaticResource BorderLight}" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="1" Opacity="0.07"/>
            </Border.Effect>
            <DataGrid x:Name="RecordsGrid" BorderThickness="0"
                      EnableRowVirtualization="True"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="日期"        Binding="{Binding DateDisplay}"                        Width="100"/>
                    <DataGridTextColumn Header="保养项目"    Binding="{Binding ComponentName}"                      Width="160"/>
                    <DataGridTextColumn Header="里程 (km)"   Binding="{Binding OdometerAtService, StringFormat=N0}" Width="110"/>
                    <DataGridTextColumn Header="备注"        Binding="{Binding Notes}"                              Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel x:Name="EmptyState" Grid.Row="1" Visibility="Collapsed"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="暂无保养记录" FontSize="18" FontWeight="Bold"
                       Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center"/>
            <TextBlock Text="在仪表盘点击「记录保养完成」即可记录" FontSize="13"
                       Foreground="{StaticResource TextSecondary}"
                       HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>
    
        <!-- Copyright footer -->
        <Border Grid.Row="2" Background="White" Padding="20,7"
                BorderBrush="{StaticResource BorderLight}" BorderThickness="0,1,0,0">
            <TextBlock Text="© Stephen Kwan  |  kwan.stephen@outlook.com"
                       FontSize="11" Foreground="{StaticResource TextSecondary}"
                       HorizontalAlignment="Right"/>
        </Border>
    </Grid>
</UserControl>
